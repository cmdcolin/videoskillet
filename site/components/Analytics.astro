---
// Google Analytics on the static pages, once the visitor has said yes. The id,
// the notice that asks and the loader are in src/analytics.ts, which the app's
// three pages call from their bundle too.
---

<script>
  import { startAnalytics } from '../../src/analytics'

  // CROSS_REPO_SYNC(analytics-snippet)
  startAnalytics()
  // CROSS_REPO_SYNC_END(analytics-snippet)
</script>
